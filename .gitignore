# Build artifacts
build/
*.o
*.a

# Datasets (too large for git)
data/

# Benchmark results — CSVs and RESULTS.md tracked, binary ground truth ignored
benchmarks/results/*.bin

# macOS junk
.DS_Store
