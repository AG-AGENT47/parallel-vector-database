# Build artifacts
build/
*.o
*.a

# Datasets (too large for git)
data/

# Benchmark results (CSV logs and binary ground truth) — not tracked
benchmarks/results/*.csv
benchmarks/results/*.bin

# macOS junk
.DS_Store
